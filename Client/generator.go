package Client
/*

import (
	pb "awesomeProject4/protobuf/algorithms"
	"math/rand"
)

func GeneratePoints(numPoints int, dimension int) ([]*pb.Point, int) {
	points := make([]*pb.Point, numPoints)
	for i := range points {
		points[i] = &pb.Point{}
		points[i].Coordinates = make([]float32, dimension)
		for j := range points[i].Coordinates {
			points[i].Coordinates[j] = rand.Float32()
		}
	}
	return points, rand.Intn(99) + 2
}
*/
